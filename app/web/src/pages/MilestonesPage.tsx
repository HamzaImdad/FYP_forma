// Session 4: MilestonesPage shows active goals + badges + milestone timeline.
// Protected by <ProtectedRoute> in App.tsx.

import { MilestonesShell } from "@/components/milestones/MilestonesShell";

export function MilestonesPage() {
  return <MilestonesShell />;
}
